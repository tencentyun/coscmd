sudo: false
language: python

python:
  - "2.6"
  - "2.7"

install:
    - pip install tox
    - pip install pep8
    - pip install requests
    - pip install nose

script:
    - pep8 --max-line-length=180
    - nosetests -s -v
    - tox
